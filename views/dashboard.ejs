<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Dashboard - Maa IMS</title>
    <link rel="stylesheet" href="../stylesheets/dashboard.css" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
    />
    <style>
      .card {
        transition: transform 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
        border: none;
        border-radius: 12px;
      }
      /* .card:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15) !important;
      } */
      .display-6 {
        font-size: 2.5rem;
      }
      .display-5 {
        font-size: 2rem;
      }
      @media (max-width: 768px) {
        .display-6 {
          font-size: 1.8rem;
        }
        .display-5 {
          font-size: 1.5rem;
        }
      }
      .table-responsive {
        border-radius: 8px;
      }
      .course-item:last-child {
        margin-bottom: 0 !important;
      }
    </style>
  </head>
  <body>
    <div id="main">
      <%- include('./partials/header') %>
      <div id="right">
        <%- include('./partials/right-header') %>
        <div class="dash-cnt">
          <h5 class="card-title" style="color: white; font-size: 2vmax">
            Dashboard
          </h5>
          <p class="card-text" style="color: white">
            Maa Computers > Dashboard
          </p>
        </div>

        <!-- Fees Summary Section -->
        <div
          class="container-fluid"
          style="background-color: #f1f5fa; padding: 1vmax 2vmax"
        >
          <div class="row g-3">
            <div class="col-lg-4 col-md-6 col-sm-12">
              <div class="card border-success h-100 shadow-sm">
                <div class="card-body text-center">
                  <h5 class="card-title text-success mb-3">
                    <i class="fas fa-calendar-day me-2"></i>Today's Collection
                  </h5>
                  <div class="internal-box">
                    
                    <p class="fw-bold text-success" style="font-size: 24px;">
                      ₹<%= totalFeesToday || 0 %>
                    </p>
                    <small class="text-muted">
                      <%= new Date().toLocaleDateString('en-IN', { day:
                      'numeric', month: 'long', year: 'numeric' }) %>
                    </small>
                  </div>
                </div>
              </div>
            </div>

            <div class="col-lg-4 col-md-6 col-sm-12">
              <div class="card border-primary h-100 shadow-sm">
                <div class="card-body text-center">
                  <h5 class="card-title text-primary mb-3">
                    <i class="fas fa-calendar-alt me-2"></i>Monthly Collection
                  </h5>
                  <div class="internal-box">
                    
                    <p class="fw-bold text-primary" style="font-size: 24px;">
                      ₹<%= totalFeesMonth || 0 %>
                    </p>
                    <small class="text-muted">
                      <%= new Date().toLocaleDateString('en-IN', { month:
                      'long', year: 'numeric' }) %>
                    </small>
                  </div>
                </div>
              </div>
            </div>

            <div class="col-lg-4 col-md-12 col-sm-12">
              <div class="card border-warning h-100 shadow-sm">
                <div class="card-body text-center">
                  <h5 class="card-title text-warning mb-3">
                    <i class="fas fa-calendar me-2"></i>Yearly Collection
                  </h5>
                  <div class="internal-box">
                    
                    <p class="fw-bold text-warning" style="font-size: 24px;">
                      ₹<%= totalFeesYear || 0 %>
                    </p>
                    <small class="text-muted">
                      <%= new Date().getFullYear() %>
                    </small>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- Students and Courses Section -->
        <div
          class="container-fluid"
          style="background-color: #f1f5fa; padding: 1vmax 2vmax"
        >
          <div class="row g-3">
            <div class="col-lg-8 col-md-12">
              <div class="row g-3">
                <div class="col-sm-6">
                  <div class="card h-100 shadow-sm">
                    <div class="card-body text-center">
                      <h5 class="card-title text-info mb-3">
                        <i class="fas fa-user-graduate me-2"></i>Students
                      </h5>
                      <div class="internal-box">
                        <h6 class="text-muted mb-2" style="font-weight: 400">
                          Total Students
                        </h6>
                        <p class="display-5 fw-bold text-info mb-0">
                          <%= student.length %>
                        </p>
                      </div>
                    </div>
                  </div>
                </div>
                <div class="col-sm-6">
                  <div class="card h-100 shadow-sm">
                    <div class="card-body text-center">
                      <h5 class="card-title text-secondary mb-3">
                        <i class="fas fa-book me-2"></i>Courses
                      </h5>
                      <div class="internal-box">
                        <h6 class="text-muted mb-2" style="font-weight: 400">
                          Total Courses
                        </h6>
                        <p class="display-5 fw-bold text-secondary mb-0">
                          <%= courses.length %>
                        </p>
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            </div>

            <div class="col-lg-4 col-md-12">
              <div class="card h-100 shadow-sm">
                <div class="card-body">
                  <h6 class="card-title fw-bold mb-3 text-center">
                    <i class="fas fa-graduation-cap me-2"></i>Our Top Courses
                  </h6>
                  <div class="course-list">
                    <% courses.slice(0, 3).forEach((course, index) => { %>
                    <div class="course-item mb-3 p-2 bg-light rounded">
                      <div
                        class="d-flex justify-content-between align-items-center"
                      >
                        <div>
                          <h6 class="mb-1 text-primary">
                            <%= course.courseName %>
                          </h6>
                          <small class="text-muted">
                            <i class="fas fa-clock me-1"></i>
                            <%= course.courseDuration %> Months
                          </small>
                        </div>
                        <span class="badge bg-primary rounded-pill"
                          ><%= index + 1 %></span
                        >
                      </div>
                    </div>
                    <% }) %>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- Daily Report Section -->
        <div
          class="container-fluid"
          style="background-color: #f1f5fa; padding: 1vmax 2vmax"
        >
          <div class="row">
            <div class="col-12">
              <div class="card shadow-sm">
                <div class="card-header bg-primary text-white">
                  <h5 class="mb-0">
                    <i class="fas fa-chart-line me-2"></i>Recent Payment
                    Transactions
                  </h5>
                </div>
                <div class="card-body p-0">
                  <div class="table-responsive">
                    <table class="table table-hover mb-0">
                      <thead class="table-light">
                        <tr>
                          <th scope="col" class="px-3 py-3">
                            <i class="fas fa-hashtag me-1"></i>R_No.
                          </th>
                          <th scope="col" class="px-3 py-3">
                            <i class="fas fa-user me-1"></i>Student Name
                          </th>
                          <th
                            scope="col"
                            class="px-3 py-3 d-none d-md-table-cell"
                          >
                            <i class="fas fa-phone me-1"></i>Contact
                          </th>
                          <th
                            scope="col"
                            class="px-3 py-3 d-none d-lg-table-cell"
                          >
                            <i class="fas fa-credit-card me-1"></i>Payment
                            Method
                          </th>
                          <th scope="col" class="px-3 py-3">
                            <i class="fas fa-calendar me-1"></i>Date
                          </th>
                          <th scope="col" class="px-3 py-3 text-end">
                            <i class="fas fa-rupee-sign me-1"></i>Amount
                          </th>
                        </tr>
                      </thead>
                      <tbody>
                        <% if (fees && fees.length > 0) { %> <% fees.slice(0,
                        5).forEach((fee, i) => { %>
                        <tr>
                          <th
                            scope="row"
                            class="px-3 py-3 text-primary fw-bold"
                          >
                            <%= fee.student.r_no %>
                          </th>
                          <td class="px-3 py-3">
                            <div class="d-flex align-items-center">
                              <div
                                class="bg-primary rounded-circle text-white d-flex align-items-center justify-content-center me-2"
                                style="
                                  width: 32px;
                                  height: 32px;
                                  font-size: 0.8rem;
                                "
                              >
                                <%=
                                fee.student.firstName.charAt(0).toUpperCase() %>
                              </div>
                              <span><%= fee.student.firstName %></span>
                            </div>
                          </td>
                          <td
                            class="px-3 py-3 d-none d-md-table-cell text-muted"
                          >
                            <i class="fas fa-phone-alt me-1"></i><%=
                            fee.student.contactNumber %>
                          </td>
                          <td class="px-3 py-3 d-none d-lg-table-cell">
                            <span class="badge bg-secondary"
                              ><%= fee.registrationPaymentMode %></span
                            >
                          </td>
                          <td class="px-3 py-3 text-muted">
                            <%= new
                            Date(fee.payDate).toLocaleDateString('en-IN', { day:
                            '2-digit', month: 'short', year: 'numeric' }) %>
                          </td>
                          <td class="px-3 py-3 text-end">
                            <span class="badge bg-success fs-6 px-3 py-2">
                              ₹<%= fee.payment %>
                            </span>
                          </td>
                        </tr>
                        <% }) %> <% } else { %>
                        <tr>
                          <td colspan="6" class="text-center py-4 text-muted">
                            <i class="fas fa-info-circle me-2"></i>No payment
                            records found
                          </td>
                        </tr>
                        <% } %>
                      </tbody>
                    </table>
                  </div>
                  <div
                    class="card-footer bg-light d-flex justify-content-between align-items-center"
                  >
                    <small class="text-muted">
                      Showing recent <%= fees ? Math.min(5, fees.length) : 0 %>
                      transactions
                    </small>
                    <a href="/feesManagement" class="btn btn-primary btn-sm">
                      <i class="fas fa-eye me-1"></i>View All Transactions
                    </a>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- Footer -->
        <div
          class="dash-cnt"
          style="
            color: white;
            display: flex;
            justify-content: center;
            align-items: center;
            font-size: 0.8vmax;
          "
        >
          2024 © Maa IMS - Designed and Power by Maa-Technology
        </div>
      </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
