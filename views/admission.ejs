<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    />

    <style>
      * {
        margin: 0%;
        padding: 0%;
        box-sizing: border-box;
        font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
          Roboto, Oxygen, Ubuntu, Cantarell, "Open Sans", "Helvetica Neue",
          sans-serif;
      }
      html,
      body {
        height: 100%;
        width: 100%;
      }
      #main {
        width: 100%;
        height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
      }
      #left {
        width: 18%;
        height: 100%;
        padding: 12px;
        border-right: 1px solid #dadada;
        background-color: white;
      }
      .cnt {
        width: 100%;
        height: 3vmax;
        padding: 2px;
        padding-left: 2vmax;
        display: flex;
        align-items: center;
        margin-top: 1vmax;
        gap: 1vmax;
      }
      .cnt > ion-icon {
        font-size: 1.4vmax;
        color: black;
      }
      .cnt > h3 {
        font-weight: 400;
        color: #315a51;
        text-transform: uppercase;
        font-size: 1.2vmax;
      }
      .cnt:hover {
        background-color: #f1f5fa;
      }
      #nav-logo {
        width: 100%;
        height: 7.4vmax;
      }
      #nav-logo img {
        width: 100%;
        height: 100%;
        object-fit: cover;
      }
      .container {
        width: 100%;
        height: 8rem;
        display: flex;
      }
      .lo {
        width: 70%;
        height: 100%;
        display: flex;
        align-items: center;
        justify-content: flex-end;
        font-size: 2vmax;
        padding-right: 6vmax;
      }
      .ro {
        width: 30%;
        height: 100%;
        display: flex;
        gap: 3.5vmax;
        padding-left: 2vmax;
        align-items: center;
        justify-content: center;
      }
      .ro > ion-icon {
        font-size: 1.5vmax;
      }
      #right {
        width: 82%;
        height: 100%;
        overflow-y: auto;
      }
      #cover {
        width: 100%;
        height: fit-content;
        background-color: #f1f5fa;

        padding: 1vmax;
      }
      .inq {
        width: 100%;
        height: fit-content;
        border-radius: 10px;
        background-color: rgb(255, 255, 255);
        margin-top: 2vmax;
        padding: 10px;
      }
       @media (max-width: 768px) {
        #right {
          width: 100%;
        }
      }
    </style>
  </head>
  <body>
    <div id="main">
      <%- include('./partials/header') %>
      <div id="right">
        <%- include('./partials/right-header') %>
        <div id="cover">
          <h1>Admission Form</h1>
          <div class="inq">
            <form action="/admission" method="post" class="row g-3">
              <div class="col-md-6">
                <label for="inputEmail4" class="form-label">Student Name</label>
                <input
                  required
                  type="text"
                  placeholder="Enter First Name"
                  name="firstName"
                  class="form-control"
                  id="inputEmail4"
                  value="<%= typeof student !== 'undefined' ? student.firstName : '' %>"
                />
              </div>
              <div class="col-md-6">
                <label for="inputPassword4" class="form-label"
                  >Student Surname</label
                >
                <input
                  required
                  type="text"
                  name="lastName"
                  placeholder="Enter Last Name"
                  class="form-control"
                  id="inputPassword4"
                  value="<%= typeof student !== 'undefined' ? student.lastName : '' %>"
                />
              </div>

              <div class="col-12">
                <label for="inputAddress" class="form-label"
                  >Name of Father / Husband</label
                >
                <input
                  required
                  type="text"
                  name="fatherName"
                  class="form-control"
                  id="inputAddress"
                  placeholder="Enter Name Of Father / Husband"
                  value="<%= typeof student !== 'undefined' ? student.fatherName : '' %>"
                />
              </div>
              <div class="col-12">
                <label for="inputAddress" class="form-label"
                  >Name of Mother</label
                >
                <input
                  type="text"
                  name="motherName"
                  class="form-control"
                  id="inputAddress"
                  placeholder="Enter Name Of Mother"
                  value="<%= typeof student !== 'undefined' ? student.motherName : '' %>"
                />
              </div>
              <div class="col-12">
                <label for="inputAddress2" class="form-label"
                  >Contact Number/ WhatsApp Number</label
                >
                <input
                  required
                  type="number"
                  name="contactNumber"
                  class="form-control"
                  id="inputAddress2"
                  placeholder="Enter Contact Number"
                  value="<%= typeof student !== 'undefined' ? student.contactNumber : '' %>"
                />
              </div>
              <div class="col-md-6">
                <label for="inputCity" class="form-label"
                  >Academic Qualification</label
                >
                <input
                  required
                  name="qualification"
                  type="text"
                  class="form-control"
                  placeholder="Enter Qualification"
                  id="inputCity"
                  value="<%= typeof student !== 'undefined' ? student.qualification : '' %>"
                />
              </div>
              <div class="col-md-2">
                <label for="inputZip" class="form-label">Date Of Joining</label>
                <input
                  name="joiningDate"
                  required
                  type="date"
                  placeholder="Address"
                  class="form-control"
                  id="inputZip"
                  value="<%= typeof student !== 'undefined' ? student.joiningDate : '' %>"
                />
              </div>
              <div class="col-md-2">
                <label for="inputZip" class="form-label">Date Of Birth</label>
                <input
                  name="dateOfBirth"
                  required
                  type="date"
                  class="form-control"
                  id="inputZip"
                  value="<%= typeof student !== 'undefined' ? student.dateOfBirth : '' %>"
                />
              </div>
              <div class="inq">
                <div class="col-md-4" style="margin-bottom: 1vmax">
                  <label for="male">Male</label>
                  <input
                    required
                    type="radio"
                    id="male"
                    name="gender"
                    value="male"
                    <%= typeof student !== 'undefined' && student.gender === 'male' ? 'checked' : '' %>
                  />

                  <label for="female">Female</label>
                  <input
                    required
                    type="radio"
                    id="female"
                    name="gender"
                    value="female"
                    <%= typeof student !== 'undefined' && student.gender === 'female' ? 'checked' : '' %>
                  />
                </div>

                <div class="row g-3">
                  <div class="col-md-12">
                    <label for="inputEmail4" class="form-label"
                      >Select Course</label
                    >
                    <select name="course" class="form-select" required>
                      <option disabled>Choose a course...</option>
                      <% if(typeof shortCourses !== 'undefined' && shortCourses.length > 0) { %>
                        <% shortCourses.forEach(course => { %>
                        <option 
                          value="<%= course._id %>"
                          <%= typeof student !== 'undefined' && student.course && student.course._id === course._id.toString() ? 'selected' : '' %>
                        >
                          <%= course.courseName %>
                        </option>
                        <% }) %>
                      <% } %>
                    </select>
                  </div>
                  <div class="col-md-12">
                    <label for="inputEmail4" class="form-label"
                      >Permanent Address</label
                    >
                    <input
                      required
                      name="address"
                      type="text"
                      class="form-control"
                      placeholder="Enter Permanent Address"
                      id="inputCity"
                      value="<%= typeof student !== 'undefined' ? student.address : '' %>"
                    />
                  </div>
                </div>
                <div class="row g-3" style="margin-top: 1vmax">
                  <div class="col-md-6" style="margin-top: 1vmax">
                    <label for="inputState" class="form-label"
                      >Registration Payment:</label
                    >
                    <select
                      name="registrationPayment"
                      id="inputState"
                      class="form-select"
                    >
                      <option <%= typeof student !== 'undefined' && student.registrationPayment === 'Done' ? 'selected' : 'selected' %>>Done</option>
                      <option <%= typeof student !== 'undefined' && student.registrationPayment === 'Not Done' ? 'selected' : '' %>>Not Done</option>
                    </select>
                  </div>
                  <div class="col-md-6">
                    <label for="inputState" class="form-label"
                      >Registration Payment Mode:</label
                    >
                    <select
                      name="registrationPaymentMode"
                      id="inputState"
                      class="form-select"
                    >
                      <option value="Cash" <%= typeof student !== 'undefined' && student.registrationPaymentMode === 'Cash' ? 'selected' : 'selected' %>>Cash</option>
                      <option value="Phone pe" <%= typeof student !== 'undefined' && student.registrationPaymentMode === 'Phone pe' ? 'selected' : '' %>>Phone pe</option>
                      <option value="Google pay" <%= typeof student !== 'undefined' && student.registrationPaymentMode === 'Google pay' ? 'selected' : '' %>>Google pay</option>
                      <option value="Paytm" <%= typeof student !== 'undefined' && student.registrationPaymentMode === 'Paytm' ? 'selected' : '' %>>Paytm</option>
                      <option value="Cheque" <%= typeof student !== 'undefined' && student.registrationPaymentMode === 'Cheque' ? 'selected' : '' %>>Cheque</option>
                    </select>
                  </div>
                </div>
                <div class="row g-3" style="margin-top: 1vmax">
                  <div class="col-md-6">
                    <label for="inputEmail4" class="form-label"
                      >Installment</label
                    >
                    <input
                      required
                      type="number"
                      name="installment"
                      class="form-control"
                      id="inputEmail4"
                      placeholder="Enter Installment Amount"
                      value="<%= typeof student !== 'undefined' ? student.installment : '' %>"
                    />
                  </div>
                  <div class="col-md-6" style="margin-top: 1vmax">
                    <label for="inputPassword4" class="form-label">Due</label>
                    <input
                      required
                      type="number"
                      name="due"
                      class="form-control"
                      id="inputPassword4"
                      placeholder="Enter Due Amount"
                      value="<%= typeof student !== 'undefined' ? student.due : '' %>"
                    />
                  </div>
                </div>

                <div class="col-md-6" style="margin-top: 1vmax">
                  <label for="inputPassword4" class="form-label"
                    >Due Amount Date</label
                  >
                  <input
                    required
                    type="date"
                    name="dueDate"
                    class="form-control"
                    id="inputPassword4"
                    placeholder="Enter Due Amount date"
                    value="<%= typeof student !== 'undefined' ? student.dueDate : '' %>"
                  />
                </div>

                <div class="col-12" style="margin-top: 1vmax">
                  <button type="submit" class="btn btn-primary">Submit</button>
                </div>
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>